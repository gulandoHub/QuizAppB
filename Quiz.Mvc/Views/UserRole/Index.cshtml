@model IEnumerable<QuizMvc.Models.UserRoleData>

@{
    ViewData["Title"] = "UserRoles";
    Layout = "_Layout";
}

<table class="table table-sm table-striped">
    <thead>
    <tr><th>User Name</th><th>Role Name</th><th>Actions</th></tr>
    </thead>
    <tbody>
    @foreach (var userRole in Model) {
        <tr>
            <td>@userRole.UserName</td>
            <td>@userRole.RoleName</td>
            <td>
                <form asp-action="Delete" method="post">
                    <a asp-action="Edit"
                       class="btn btn-sm btn-warning" asp-route-id="@userRole.ID">
                        Edit
                    </a>
                    <input type="hidden" name="id" value="@userRole.ID" />
                    <button type="submit" class="btn btn-danger btn-sm">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>

<div class="text-center">
    <a asp-action="Create" class="btn btn-primary">Create New UserRole</a>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Go To Home</a>
</div>