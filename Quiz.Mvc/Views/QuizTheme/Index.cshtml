@model IEnumerable<QuizData.QuizThemeSummary>
@{
    ViewData["Title"] = "Quiz Themes";
    Layout = "_Layout";
}
<table class="table table-sm table-striped">
    <thead>
    <tr><th>Quiz Name</th><th>Quiz Theme Name</th><th>Actions</th></tr>
    </thead>
    <tbody>
    @foreach (var quiztheme in Model) {
        <tr>
            <td>@quiztheme.QuizName</td>
            <td>@quiztheme.QuizThemeName</td>
            <td>
                <form asp-action="Delete" method="post">
                    <a asp-action="Edit"
                       class="btn btn-sm btn-warning" asp-route-id="@quiztheme.ID">
                        Edit
                    </a>
                    <input type="hidden" name="id" value="@quiztheme.ID" />
                    <button type="submit" class="btn btn-danger btn-sm">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>

<div class="text-center">
    <a asp-action="Create" class="btn btn-primary">Create New Quiz Theme</a>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Go To Home</a>
</div>