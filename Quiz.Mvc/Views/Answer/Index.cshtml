@model IEnumerable<QuizMvc.Models.AnswerData>
@{
    ViewData["Title"] = "Answers";
    Layout = "_Layout";
}
<table class="table table-sm table-striped">
    <thead>
    <tr><th>Answer Type Name</th><th>Answer Text</th><th>Actions</th></tr>
    </thead>
    <tbody>
    @foreach (var answer in Model) {
        <tr>
            <td>@answer.AnswerTypeName</td>
            <td>@answer.AnswerText</td>
            <td>
                <form asp-action="Delete" method="post">
                    <a asp-action="Edit"
                       class="btn btn-sm btn-warning" asp-route-id="@answer.ID">
                        Edit
                    </a>
                    <input type="hidden" name="id" value="@answer.ID" />
                    <button type="submit" class="btn btn-danger btn-sm">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>

<div class="text-center">
    <a asp-action="Create" class="btn btn-primary">Create New Answer</a>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Go To Home</a>
</div>

